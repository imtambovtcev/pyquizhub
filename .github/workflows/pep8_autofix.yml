name: PEP8 Auto-fix on Main

on:
  push:
    branches:
      - main
    paths:
      - '**/*.py'

permissions:
  contents: write
  pull-requests: write

jobs:
  autofix-formatting:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.x

    - name: Install autopep8
      run: pip install autopep8

    - name: Auto-fix formatting
      run: |
        FILES=$(find . -name "*.py")
        echo "Auto-fixing formatting..."
        for file in $FILES; do
            autopep8 --in-place --aggressive --aggressive $file
        done

    - name: Check for changes
      id: verify_diff
      run: |
        git diff --quiet . || echo "changed=true" >> "$GITHUB_OUTPUT"

    - name: Commit and push to auto-format branch
      if: steps.verify_diff.outputs.changed == 'true'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git checkout -B auto-format
        git add -A
        git commit -m "style: auto-fix PEP8 formatting"
        git push -u origin auto-format

    - name: Open or update Pull Request
      if: steps.verify_diff.outputs.changed == 'true'
      uses: peter-evans/create-pull-request@v5
      with:
        branch: auto-format
        title: "style: automated PEP8 formatting"
        body: "This PR applies automated PEP8 formatting fixes."

